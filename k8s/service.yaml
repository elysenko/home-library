apiVersion: v1
kind: Service
metadata:
  name: home-library
  namespace: colossus-home-library
  labels:
    app: home-library
    managed-by: colossus
spec:
  type: ClusterIP
  selector:
    app: home-library
  ports:
    - port: 80
      targetPort: 80
