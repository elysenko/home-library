apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: home-library-staging
  namespace: colossus-home-library-staging
  labels:
    app: home-library-staging
    managed-by: colossus
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: ubuntu.desmana-truck.ts.net
      http:
        paths:
          - path: /home-library-staging(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: home-library-staging
                port:
                  number: 80
  tls:
    - hosts:
        - ubuntu.desmana-truck.ts.net
      secretName: colossus-shared-tls
